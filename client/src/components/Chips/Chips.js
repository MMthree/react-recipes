import React from "react";

const Chips = props => (
            <div className="container">
                <div className="row">
                    <button>{props.searchTerms}</button>
                </div>
            </div>
)

export default Chips;